#include "main.h"
#include <stdio.h>
/**
 * is_palindrome - A function that returns 1 if a string is a palindrome and 0
 * if not
 * @s: String parameter
 * Return; Return 1 or 0
 */
//int is_palindrome(char *s)
//{

//}

int main(void)
{
	printf("%s", reverse("Ade"));
	return (0);
}
char *reverse(char *str, char * dst)
{
	if (*s == '\0')
	{
		return;
	}
	reverse((str + 1), dst);
	

}
