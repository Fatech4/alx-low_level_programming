#include "main.h"
/**
 * mul -  A function that multiplies two integers.
 * @a: First number
 * @b: Second number
 * Return: Return the product of the two numbers
 */
int mul(int a, int b)
{
	return (a * b);
}
