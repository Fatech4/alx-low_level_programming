#include "main.h"

/**
 * _isupper - A function that checks for uppercase character.
 * @c: Function parameter
 * Return: Return 1 or 0
 */
int _isupper(int c)
{
	if ((c >= 65) && (c <= 90))
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
